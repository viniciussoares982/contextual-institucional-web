import { slugify } from "@/app/utils/utils";

export default function Header() {
  return (
    <nav className="max-w-8xl mx-auto px-7 sm:px-6 md:px-8 md:w-[722px] flex flex-col justify-start items-center h-full">
      asdasd
    </nav>
  )
}