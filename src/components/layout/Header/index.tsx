import { slugify } from "@/app/utils/utils";

export default function Header() {
  return (
    <nav className="max-w-8xl mx-auto flex h-full flex-col items-center justify-start px-7 sm:px-6 md:w-[722px] md:px-8">
      asdasd
    </nav>
  );
}
